version: '3.4'

services:
  intive.patronage2023.api:
    image: ${DOCKER_REGISTRY-}intivepatronage2023api
    build:
      context: .
      dockerfile: src/api/app/Dockerfile
    ports:
      - "7106:443"

  db:
    image: "mcr.microsoft.com/mssql/server:2022-latest"
    ports:
      - "1433:1433"
    environment:
      SA_PASSWORD: "S3cur3P@ssW0rd!"
      ACCEPT_EULA: "Y"